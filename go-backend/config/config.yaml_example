app:
  port: "8080"

blockchain:
  node_url: "https://mock-chain.io"
  private_key: "mock_private_key_here"

pinata:
  jwt_key : "Bearer JWT_KEY_HERE"

cors:
  allowed_origins: "http://localhost:3000"